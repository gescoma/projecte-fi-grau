FROM python:3-alpine

WORKDIR /workspaces/edelweiss-server

RUN pip install --upgrade pip

COPY requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt

CMD ["uvicorn", "webAPI:app", "--reload", "--host", "0.0.0.0"]